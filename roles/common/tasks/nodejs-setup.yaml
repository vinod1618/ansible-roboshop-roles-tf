- name: disable dnf module and enable node js 20
  ansible.builtin.command: dnf module enable nodejs:20 -y

- name: install nodejs 20
  ansible.builtin.dnf:
    name: nodejs
    state: present

- name: install dependencies
  community.general.npm:
    path: /app